{
  "name": "naviga-life-site",
  "version": "1.0.0",
  "private": true,
  "description": "Static product site and catalogue build for Naviga Life",
  "scripts": {
    "build": "node src/build.js",
    "serve": "npm run build && node src/serve.js"
  },
  "devDependencies": {
    "puppeteer": "^24.0.0",
    "sharp": "^0.34.0"
  }
}
