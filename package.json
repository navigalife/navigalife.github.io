{
  "name": "medivasc-site",
  "version": "1.0.0",
  "private": true,
  "description": "MediVasc static site build — prevention of foot and leg amputation",
  "scripts": {
    "build": "node src/build.js",
    "pdfs": "node tools/gen-pdfs.js",
    "serve": "npm run build && npm run pdfs && node src/serve.js"
  },
  "devDependencies": {
    "puppeteer": "^24.0.0",
    "sharp": "^0.34.0"
  }
}
